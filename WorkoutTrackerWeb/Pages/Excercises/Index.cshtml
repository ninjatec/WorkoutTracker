@page
@model WorkoutTrackerWeb.Pages.Excercises.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Excercise[0].ExcerciseName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Excercise) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ExcerciseName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ExcerciseId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ExcerciseId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ExcerciseId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
